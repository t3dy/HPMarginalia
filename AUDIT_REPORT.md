# Audit Report

Generated: 2026-03-18T20:09:27.623426

## What Changed (V2 Migration)

### Schema
- Added `annotations` table for first-class marginal note records
- Added `annotators` table (normalized from `annotator_hands`)
- Added `doc_folio_refs` table (generalized from `dissertation_refs`)
- Added `dictionary_terms` and `dictionary_term_links` tables
- Added `document_topics` junction table for multi-value topic clusters
- Added `review_status`, `needs_review`, `reviewed`, `source_method`, `confidence`, and `notes` columns across existing tables
- Downgraded all BL C.60.o.12 matches from MEDIUM to LOW confidence

### Site
- 30 scholar profile pages (DB-driven, with review badges)
- 38 dictionary term pages (37 terms, 6 categories, 76 links)
- 109 marginalia folio detail pages (with annotator hand info)
- Site-wide navigation bar (Home, Marginalia, Scholars, Dictionary, About)
- About page with database statistics and rebuild instructions
- Confidence badges (HIGH/MEDIUM/LOW/PROVISIONAL) on all matches
- Review badges (Unreviewed) on LLM-assisted content

## What Remains Provisional

| Data Category | Total | Needs Review | Source Method |
|---|---|---|---|
| Bibliography entries | 88 | 109 | LLM-assisted |
| Scholar profiles | 52 | 60 | LLM-assisted |
| Dictionary terms | 37 | 37 | LLM-assisted |
| Image matches | 610 | 584 | Algorithmic (folio mapping) |
| BL matches specifically | 218 | 218 | LOW confidence (1545 edition offset) |

## What Still Needs Human Review

### Critical
1. **BL C.60.o.12 photo-to-folio mapping**: Verify that sequential photo numbers
   correspond to folio numbers. The BL copy is the 1545 edition; layout may differ
   from the 1499 signature map by a few leaves.
2. **Article summaries**: All 34 summaries were generated by Claude and have not
   been checked for factual accuracy, misattribution, or hallucination.
3. **Hand attributions**: Derived from reading Russell's prose in conversation.
   The signature-to-hand mapping rules are approximate.

### Important
4. **Scholar metadata**: Birth/death years, nationalities, and institutional
   affiliations should be cross-referenced against VIAF/WorldCat.
5. **Dictionary definitions**: Especially the alchemical and architectural terms
   should be reviewed by a domain specialist.
6. **Mislabeled files**: Jarzombek (De pictura, not HP) and Canone/Spruit
   (Poncet on Botticelli, not emblematics) were identified by LLM and
   should be confirmed.

### Nice to Have
7. **Timeline event dates**: Some date ranges are approximate.
8. **Topic cluster assignments**: Some works could belong to multiple clusters.
   The `document_topics` junction table supports this but most entries
   have only one topic assigned.

## Validation Warnings

- SCHOLAR NO WORKS: 'Aldus Manutius' has no linked bibliography entries
- SCHOLAR NO WORKS: 'Ben Jonson' has no linked bibliography entries
- SCHOLAR NO WORKS: 'Benedetto Giovio' has no linked bibliography entries
- SCHOLAR NO WORKS: 'Fabio Chigi (Pope Alexander VII)' has no linked bibliography entries
- SCHOLAR NO WORKS: 'Jean d'Espagnet' has no linked bibliography entries
- SCHOLAR NO WORKS: 'Paolo Giovio' has no linked bibliography entries
- SCHOLAR NO WORKS: 'Robert Dallington' has no linked bibliography entries
- UNMATCHED REFS: 26 dissertation references have no image match

## How to Rebuild

```bash
python scripts/migrate_v2.py        # Schema migration (idempotent)
python scripts/seed_dictionary.py   # Dictionary terms (idempotent)
python scripts/build_site.py        # Generate all HTML + JSON
python scripts/validate.py          # Run this audit
```
